# Core API Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database (SQLite via SQLAlchemy)
sqlalchemy==2.0.35
aiosqlite==0.20.0

# Pydantic for data validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Security & Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==43.0.1

# Config / YAML parsing (service specs)
pyyaml==6.0.2
python-dotenv==1.0.1

# NLU / LLM Integration
httpx==0.27.2
requests==2.32.3

# Language Detection
langdetect==1.0.9

# Fraud/Anomaly Scoring (pure Python rule-based for POC, no numpy required)
# scikit-learn and numpy optional for production LightGBM integration

# Document Processing (mock OCR)
Pillow==10.4.0

# Async task support
anyio==4.6.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# Logging & Observability
structlog==24.4.0
